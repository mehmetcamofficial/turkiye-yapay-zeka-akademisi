# Metric History

`history.json` contains only confirmed values with source and confidence.
`releases.json` contains immutable releases evidenced by Git tags. Add snapshots
only when a small canonical historical payload is required.

