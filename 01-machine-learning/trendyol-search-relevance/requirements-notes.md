# Environment Notes

The project uses the repository root `.venv`: Python, pandas, NumPy, SciPy, scikit-learn, PyArrow, joblib and matplotlib. No new dependency or broad upgrade is required. Sparse feature matrices remain sparse; full mode is never started automatically.
