# V2 optional runtime

V2 ranking experiments use `xgboost==3.3.0`, installed into the repository `.venv`. CatBoost and transformer stacks are not required. They were absent during the audit and were not downloaded.
