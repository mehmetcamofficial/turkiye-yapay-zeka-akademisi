# Documentation Update Prompt Template

Metadata: purpose=knowledge update; version=1; related_task=<ID>;
related_result=<docs>; status=template; limitations=evidence only.

Update current state, project, task, and journal. Add conditional records only
when triggered. Validate claims, links, YAML/JSON, dates, SHAs, and scope.

