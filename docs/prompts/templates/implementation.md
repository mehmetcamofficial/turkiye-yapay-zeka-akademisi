# Implementation Prompt Template

Metadata: purpose=bounded implementation; version=1; related_task=<ID>;
related_result=<record>; status=template; limitations=explicit scope only.

Implement the approved hypothesis in the listed files. Preserve protected
assets and invariants. Add deterministic tests, validate in increasing scope,
audit the diff, and report documentation impact. Do not commit without approval.

