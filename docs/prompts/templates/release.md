# Release Prompt Template

Metadata: purpose=release verification; version=1; related_task=<ID>;
related_result=<release>; status=template; limitations=no push/tag by default.

Verify branch/HEAD, exact staged files, diff checks, tests, canonical metrics,
protected hashes, release notes, and rollback conditions. Commit/tag/push only
when explicitly authorized.

