# Figures Inventory

- Existing tracked screenshots: `docs/assets/showcase/`.
- Existing model plots: project `outputs/` directories.
- Candidate diagrams: system layers, model lifecycle, Trendyol V1–V5 pipeline,
  Copilot evaluation loop, and knowledge graph.

Every published figure needs a source path, commit/version, caption, metric
definition where applicable, and confirmation that no sensitive data appears.

The canonical planning/status source is `../assets/manifest.yaml`. Publication
priorities include architecture evolution, generated knowledge graph, generated
metrics dashboard, current portfolio/search/Copilot captures, and verified
standalone application captures. No placeholder is itself an image.
