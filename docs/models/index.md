# Model and Algorithm Index

The machine-readable inventory is `model-inventory.yaml`. It distinguishes
selected predictive models, evaluated challengers, retrieval/ranking methods,
heuristic scorers, and evaluation components. Dependency presence alone is not
implementation evidence.

