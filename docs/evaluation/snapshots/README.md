# Evaluation Snapshots

Store small, intentional, immutable metric snapshots here when they are needed
for historical comparison. Do not duplicate generated benchmark output or
large acceptance artifacts. Machine-readable summary history lives in
`metrics/history.json`.

